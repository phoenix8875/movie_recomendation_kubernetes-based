apiVersion: v2
name: movie-watchlist
description: 3-tier movie app (Nginx frontend, FastAPI backend, Postgres db) on k3s
type: application

# version  = the version of THIS CHART (the packaging). Bump when you change templates.
# appVersion = the version of the APP inside it. Tracks your container image tags.
# They are deliberately separate: you can fix a typo in a template (chart 0.1.1)
# without claiming the app changed (appVersion still 2.0).
version: 0.1.0
appVersion: "2.0"
